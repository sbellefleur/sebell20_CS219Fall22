package Interface_Example;

public interface HW0_Specification {
    // in an interface everything is public by default so we don't need to specify
    // return the sum of the digits in n
    int sod(int n);
}
